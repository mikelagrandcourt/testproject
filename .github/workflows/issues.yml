name: Issues
on:
  issues:
    types: [opened]
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Output github issue details
        run: echo "The event ${{ toJSON(github.issue) }} triggered this workflow"